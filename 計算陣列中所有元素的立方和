#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number[6];
    int sum = 0;
    for(int i=0;i<6;i++)
    {
        scanf("%d",&number[i]);
    }
    for(int i=0;i<6;i++)
    {
        sum = sum + number[i]*number[i]*number[i];
    }
    printf("%d\n",sum);
    return 0;
}
