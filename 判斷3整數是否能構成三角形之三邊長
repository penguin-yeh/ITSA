#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    int b;
    int c;
    while(scanf("%d%d%d",&a,&b,&c)!=EOF)
    {
        if(a+b>c && b+c>a && a+c>b) printf("fit\n");
        else printf("unfit\n");
    }
    return 0;
}
